# Poker Solver

A small Python toolkit for poker hand analysis.

## Getting started

```bash
pip install -r requirements.txt
pytest
```

## Project layout

- `poker_solver/` — core library code
- `tests/` — unit tests

## Status

Early scaffold. Contributions welcome via pull request.
