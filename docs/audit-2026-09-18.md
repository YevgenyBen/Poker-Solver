# Deep-dive audit — 2026-09-18/19 (M283)

Three arms, graded against a rubric fixed **before any number landed**:
measured against ourselves, against the independent solver, and against
139,508 real played hands. The rubric, its refusal conditions and the
"what each arm cannot settle" table are reproduced in full below, because
every grade this project has granted and later withdrawn (M167, M168,
M177, M180, M260's turn) was granted against a bar chosen after seeing
the data.

**Headline: the engine is accurate and honest, and it is SLOW on first
ask at a table size and depth nobody warmed.** 1,200 of 1,200 real
decisions answered with zero defects, every street inside its own
equilibrium bar — and a player sitting 8-handed at 147bb waits **211
seconds** for their first answer.

---

## The unit: usability at the table

Not "how close to GTO". A player has this open mid-hand and needs a
decision. That decomposes into four things that fail independently.

| axis | question | grade |
|---|---|---|
| **U1** | can it answer? | **A** |
| **U2** | in time? | **C overall, F at 7- and 8-handed** |
| **U3** | right where the money is? | **B on what can be graded; the flop and turn are UNGRADED** |
| **U4** | does it say when not to trust it? | **B** |

---

## U1 — coverage: A

Share of clean real hands at a table size and depth the engine serves.

| | hands | share |
|---|---|---|
| clean real hands (>= 2bb) | 113,614 | — |
| **at a supported table size** | **113,199** | **99.63%** |
| reachable by the OLD replay filter | 88,189 | 77.62% |

Only **10-handed** (415 hands, 0.37%) is genuinely unsupported.

And in the replay itself: **1,200 of 1,200 decisions answered, 0
defects** — no unaffordable advice, no malformed distribution, no null
hero row, no premium folded preflop.

**F55 — the replay instrument was stale, and it was hiding the worst
cell.** `bench/real_replay.py` still declared
`SUPPORTED_TABLE_SIZES = (2,3,4,5,6,9)` and capped `eff_stack_bb <= 200`.
M270 shipped 7- and 8-handed; M271 warmed the deep buckets. So every
replay ever run here measured a population **22% narrower than the
product supports**, and excluded the 15.94% of real hands sitting above
200bb. Fixed in this audit — and the fix is what surfaced F56 below.
M252's F54 in a new place: *a benchmark measures the population it
generates, and nothing else.*

---

## U2 — latency: C overall, F at 7- and 8-handed

1,200 real decisions, real stacks, real lines.

| | n | p50 | p90 | p99 | max | over 5s |
|---|---|---|---|---|---|---|
| all | 1,200 | 0.413 | 7.071 | 146.7 | **211.4** | 150 (12.5%) |

**F56 — 7- AND 8-HANDED TABLES ARE WARMED AT ONE DEPTH, AND REAL STACKS
ARE NOT AT THAT DEPTH.** Severity: **high**. This is the audit's main
finding.

| table | n | p50 | p90 | max | over 5s |
|---|---|---|---|---|---|
| 2-max | 243 | 0.402 | 2.459 | 3.529 | 0 |
| 3-max | 47 | 1.985 | 3.612 | 7.058 | 1 |
| 4-max | 121 | 0.758 | 3.128 | 7.961 | 3 |
| 5-max | 284 | 0.025 | 5.116 | 19.554 | 30 |
| 6-max | 397 | 0.077 | 11.103 | 29.651 | 59 |
| **7-max** | 24 | **20.541** | 34.025 | 42.281 | 13 of 24 |
| **8-max** | 73 | **7.071** | **165.131** | **211.379** | 39 of 73 |
| 9-max | 11 | 2.783 | 10.834 | 11.342 | 5 |

Every one of the ten slowest decisions is an 8-max **preflop** solve at a
stack that is not 100bb: 146.91, 107.0, 115.3, 81.8, 139.53, 241.4,
163.1, 223.4, 121.69, 86.96 bb.

The cause is in the configuration, not the solver:

- `MULTIWAY_PREWARM_DEPTHS_BY_TABLE = {7: (100.0,), 8: (100.0,), 9: (100.0,)}`
- `MULTIWAY_BACKGROUND_WARM` holds buckets for table sizes **3, 4, 5 and
  6 only** — 37, 16, 28 and 34 of them. M271 built that warmer one
  milestone after M270 shipped 7- and 8-handed, and it was never
  extended to them.

**Exposure, against the hand store**: 7- and 8-handed are **7,745 real
hands = 6.82%**, and only **14.8%** of those sit in the single warmed
bucket. So **~5.81% of all real hands hit a cold 7/8-max preflop solve** —
higher exposure than every disclosed defect in this product except
multiway irreproducibility.

**Warm is fine; first-ask is the whole problem.**

| | n | p50 | p90 | max | over 5s |
|---|---|---|---|---|---|
| first ask at a (size, bucket) | 260 | 2.094 | 25.379 | 211.379 | 93 |
| repeat asks | 940 | **0.027** | 3.269 | 144.964 | 57 |
| excluding 7/8-max | 1,103 | 0.405 | 4.470 | 29.651 | 98 |

**No comparison is drawn against the 2026-09-08 audit's latency**, and
that is deliberate. That run warmed three depths; this one warmed one, so
the difference lands exactly in first-ask cost, which is the thing the
warming configuration controls. M240 also measured this machine drifting
**9.7x inside a single run**. A wall-clock number compared across two
audits on a drifting machine with different warming is not evidence of
anything — which is what the previous audit's own **R3** said, and R3's
instrument (`bench/reference_units.py`) was not used here either.

---

## U3 — accuracy

### Against ourselves: every street inside its own bar

Exploitability needs no reference and has no shared blind spot (M211).
12 spots per street, drawn off the tree, solved exactly as production
solves them. Zero dropped.

| street | n | median | p90 | worst | bar 1.0% |
|---|---|---|---|---|---|
| flop | 12 | **0.368%** | 0.426% | 0.561% | PASS |
| turn | 12 | **0.537%** | 0.655% | 0.733% | PASS |
| river | 12 | **0.095%** | 0.110% | 0.143% | PASS |

The turn's 0.537% replicates **M278's 0.559%** and **M233's 0.562%** on
fresh spots — three independent studies agreeing, which is the bar M166
failed.

**This settles that we solve our own model well, and nothing else.**
M233 measured the two coming apart directly: at 1,000 iterations the
engine sits 0.083% from its own equilibrium while its distance from an
independent reference GROWS. A good figure here restores no withdrawn
grade.

### Against the independent solver: less is gradeable than the record implies

| street | node type | status |
|---|---|---|
| river | opening | **A** — 0.41% of pot (M260), reference gated at 0.39% |
| river | facing a bet | **B** — 0.88% of pot (M260) |
| turn | both | **UNGRADED** — A14/M277 withdrew these; the references measure 1.87–3.91% of pot against 0.33–0.49% reported, and converging 7.2x tighter moves the dumped strategy 3.91 → 3.81% |
| flop | both | **UNGRADED — see F57** |

**F57 — the flop's external figures have never passed the gate that
withdrew the turn's.** Severity: **medium**.

A13/M276 established that the solver's reported exploitability stops
describing the strategy it dumps as chance levels deepen: a river
reference (no chance node) agrees at 1.46x, a turn reference (one) fails,
and a four-bet **flop** reference (two) measured **1.46% of pot against a
reported 0.045%** — refused. A flop reference is the deepest case and the
one never checked.

It *cannot* be checked off the dumps we hold: every flop reference in the
bank was written with `set_dump_rounds 1`, so the file contains the flop
and nothing else. Walking it would value a turn leaf at raw equity while
the strategy being scored was optimised against turn and river play the
solver ran and did not serialise — M257's F62/F67, which is the
difference between an 8.4x FAIL and a 2.5x PASS on one spot.

Gating properly needs three-round dumps: ~3.5 GB and ~8–11 minutes of
solve each at this SPR, one at a time (a resident dump takes ~4.5x its
file size in RAM). A single-spot pilot was started for this audit and
**did not complete** - the session ended before the solver wrote its dump,
so no figure exists. It is queued as **R1**, not reported as run.

**What is at stake**: M260's flop grades, and `FLOP_UNDER_FOLD_NOTE`,
which is shipped user-facing copy firing on 0.58% of real decisions.

---

## U4 — honesty: B

### Exposure, over 1,200 real decisions

| disclosure | share |
|---|---|
| *(no caveat named)* | 76.92% |
| standing aggression caveat | 23.08% |
| flop-measured | 12.17% |
| street-isolation | 11.33% |
| facing-a-bet-cost | 8.67% |
| drawy-board | 7.17% |
| street-unmeasured | 6.83% |
| turn-independent | 5.42% |
| river-measured | 4.08% |
| costly-band | 3.42% |
| bet-sizing-coverage | 0.67% |
| flop-under-fold | 0.58% |
| multiway-bet | 0.58% |
| river-under-fold | 0.42% |
| turn-shove | 0.08% |

`solver_confidence` is `"low"` on **14.25%** of decisions — and on
**100% of the 54 multiway postflop decisions**, which is exactly where
M245/M267 say it must fire. The disclosure machinery works.

*(This population is 77% preflop because the replay samples a uniform
decision from each hand; the 2026-09-08 audit's 20% headline for
multiway irreproducibility was over a more postflop-heavy population.
Multiway postflop is 19.5% of the POSTFLOP decisions here, which is the
comparable figure.)*

### F58 — the pinning tests pin the copy to the constant, and the constant to nothing

Severity: **medium**, and it fired **twice in the last two milestones**.

There are 18 `test_*_quotes_its_own_measurement` tests. Every one asserts
that the sentence a player reads contains the number a constant holds.
**None asserts that the constant still matches a re-run of the study it
came from.**

- **M281** shipped a setting that improved a disclosed defect and made
  its own disclosure false — the copy said 98% where the truth was 66%.
- **M282** then found the replacement figure was *also* wrong (0.6559
  against a population truth of 0.8081), because the study behind it was
  a hand-written list of 14 paths rather than the 84-node population the
  gate fires on.

Both passed the full suite while being wrong, because nothing in the
suite can tell a stale constant from a fresh one.

**U4 is capped at B for this reason**, not lower: the rubric caps at D
only if a warning currently understates its defect, and M282 corrected
the one that did.

---

## Direction — is this moving the right way?

Only figures measured the same way are compared. Where the method
changed, the row says so rather than showing a misleading delta.

| quantity | 2026-08-23 (M101) | 2026-09-08 (M252) | **this audit** | direction |
|---|---|---|---|---|
| real decisions replayed | live-play sim | 1,730 | 1,200 | — |
| correctness defects | 4 findings | **0** | **0** | holding |
| decisions answered | — | 98.5–97.0% | **100%** | better |
| table-size coverage of real hands | — | not measured | **99.63%** | new |
| turn exploitability (own model) | — | — | **0.537%** | replicates M233/M278 |
| disclosures with measured exposure | none | all | all | holding |
| latency | see note | see note | see note | **not comparable** |

**Genuine progress since 2026-09-08**, all of it measured elsewhere and
confirmed here: 4-, 5-, 7- and 8-handed tables shipped (M268/M270) taking
coverage from 46% to 99.63%; deep multiway warmed (M271); multiway
postflop budget x4 (M264) and grouped action matching (M269, +0.042 at
3.65σ and faster); the turn's grades honestly withdrawn rather than
quietly kept (M277); the two-live warning corrected twice (M281, M282).

**The direction is right and the pattern is consistent**: nearly every
milestone in this window either shipped coverage or withdrew a claim that
did not survive its own instrument. That is the healthy shape.

**The one thing moving the wrong way is first-ask latency**, and it is a
direct consequence of the coverage work: three new table sizes shipped
without the warming that the older sizes have.

---

## Recommendations

| # | from | action | status |
|---|---|---|---|
| **R1** | F57 | gate the flop reference bank with three-round dumps, one spot first | **queued (pilot started, did not complete)** |
| **R2** | F56 | background-warm 7- and 8-handed buckets the way M271 warmed 5- and 6-max | **DONE (M284), differently** — RAM warming does not fit (194 MB an 8-max entry), so a disk tier: first ask 89.0s -> 0.30s at 7-max, 415.5s -> 1.02s at 8-max, same advice |
| **R3** | F58 | make a disclosed constant re-derivable: record the script that produced each, and re-run them on a schedule | **queued** |
| **R4** | prior audit R3 | measure the replay in reference units, not wall clock | **queued** |
| **R5** | prior audit R2 | measure the 0–1 raise two-live cell, where the gate is SILENT | **still open (M253 could not settle it)** |
| **R6** | F53/M253 | cap total multiway work by live count, not only per-seat width | **queued, unmeasured** |

**Explicitly not recommended**: a fifth attempt at a configuration for
multiway postflop irreproducibility. M245 measured width (inert),
iterations (0.240 at 150x budget) and ensembles (worst cases untouched at
every K); M267 re-measured at the new budget. It is disclosed because it
cannot currently be fixed.

**Also not recommended**: anything aimed at M98's terminal pricing
without a new idea. Five attempts have failed (M100, M112–M116, M279),
and M280 localised the residual to the modelled four-bet range's
composition — 13.2% of hands at a 3.0% premium against a 23.2%
break-even. That is a *diagnosis*, not a fix, and the next attempt needs
a mechanism rather than another knob.

---

## The rubric, as fixed before the run

### What each arm can and cannot settle

| arm | settles | cannot settle |
|---|---|---|
| against ourselves | whether we solve OUR model well, and whether the answer is reproducible | whether the model is the game (M233 showed these come apart) |
| against the independent solver | whether our model matches a different one, where its dump passes the precision gate | the turn (no valid reference), and anything it and we both get wrong |
| against real hands | coverage, latency at real stacks, defect count, how often each warning is met | correctness — a real hand's action is one player's choice, not the right answer |

### Grade thresholds

- **U1** A >= 99% of real decisions answered, B >= 95%, C >= 90%, D >= 75%.
- **U2** A p90 < 2s and max < 5s in every cell; B p90 < 5s and max < 10s;
  C p90 < 10s; D p90 < 30s; F worse, or a cell that cannot be served.
  Cold first-ask reported separately, never averaged in.
- **U3** A < 0.5% of pot, B < 1.0%, C < 2.0%, D < 4.0%. **A cell with no
  instrument that passes its own control is UNGRADED, not given the
  benefit of the doubt.**
- **U4** A all figures current, every warning fires where measured, every
  exposure known; B one stale figure or one unmeasured exposure; **capped
  at D if any warning UNDERSTATES its defect** (M232).
