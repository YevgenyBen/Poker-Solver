"""Studies that re-derive the figures shipped copy quotes (M285).

Each exposes a pure reduction (`summarise`) that is unit-tested and a
`main` that measures through `/advise` and reports drift from
`api/config.py`. Registered in `bench/disclosures.py`.
"""
