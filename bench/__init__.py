"""Measurement instruments for this project's own studies.

Deliberately OUTSIDE the shipped packages. Nothing under `poker_solver/`
or `api/` imports anything here, and nothing here reaches a user - these
are tools for measuring the engine, in the same category as the external
solvers CLAUDE.md's "instruments, never ingredients" law governs.
"""
